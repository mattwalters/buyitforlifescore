{
  "name": "@mono/site",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "dotenv -e ../../.env -- astro dev --host 127.0.0.3 --port 3008",
    "build": "dotenv -e ../../.env -- astro build",
    "start": "dotenv -e ../../.env -- astro preview --host 127.0.0.3 --port 3008",
    "lint": "eslint .",
    "clean": "rm -rf .turbo node_modules dist .astro",
    "astro": "astro"
  },
  "dependencies": {
    "@mono/db": "*",
    "astro": "^6.1.5",
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "@mono/eslint-config": "*",
    "@types/d3": "^7.4.3",
    "prettier": "^3.8.1"
  }
}
